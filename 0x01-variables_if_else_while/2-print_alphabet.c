#include<stdio.h>
/**
 * Main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char alf;

	for (alf = 'a'; alf <= 'z'; alf++)
	{
		putchar (alf);
	}
	putchar('\n');
	return (0);
}
