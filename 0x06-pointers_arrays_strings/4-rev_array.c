#include <stdio.h>
#include "main.h"
/**
 * reverse_array - entry point
 *
 * Description: reverses arrays
 * @a: array
 * @n: length of array
 * Return: void
 */

void reverse_array(int *a, int n)
{
	int i, l;

	l = 0;
	for (i = 0; a[i] != '\0'; i++)
	{
		l++
	}
	for (
}
